// AdPreview.tsx
import React from 'react';

interface AdPreviewProps {
  adData: any;
}

const AdPreview: React.FC<AdPreviewProps> = ({ adData }) => {
  return (
    <div>
      {/* Componentes para mostrar la vista previa del anuncio usando adData */}
    </div>
  );
};

export default AdPreview;