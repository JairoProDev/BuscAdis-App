// PublicationProgress.tsx
import React from 'react';

interface PublicationProgressProps {
  progress: number;
}

const PublicationProgress: React.FC<PublicationProgressProps> = ({ progress }) => {
  return (
    <div>
      {/* Componentes para mostrar la barra de progreso y el porcentaje */}
    </div>
  );
};

export default PublicationProgress;